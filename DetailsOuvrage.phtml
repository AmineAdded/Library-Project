<!-- ======= Styles ====== -->
<link rel="stylesheet" href="assets/css/style.css">
<link rel="stylesheet" href="assets/css/styleButtonAjouter.css">

<style>
    /* Button update et supprimer */
    .ButtonsUpSupp a {
        text-decoration: none;
        padding: 10px 15px;
        border-radius: 5px;
        display: inline-flex;
        align-items: center;
        font-weight: bold;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    /* Bouton Update (jaune) */
    .btn-update {
        background-color: #f1c40f;
        color: #fff;
    }

    .btn-update:hover {
        background-color: #f39c12;
        transform: scale(1.05);
    }

    /* Bouton Delete (rouge) */
    .btn-delete {
        background-color: #e74c3c;
        color: #fff;
    }

    .btn-delete:hover {
        background-color: #c0392b;
        transform: scale(1.05);
    }

    /* Style pour les icônes */
    a i {
        margin-right: 5px;
    }
</style>

        <div class="details">
            <div class="recentOrders">
                <div class="cardHeader">
                    <h2>Liste des ouvrages</h2>
                    <a href="AjoutOuvrage.php">
                        <button class="Btn">
                            <div class="sign">+</div>

                            <div class="text">Ajouter</div>
                        </button>
                    </a>
                    <!-- <a href="#" class="btn">View All</a> -->
                </div>

                <!-- ================ LES TABLEAUX ICI ================= -->

                <table>
                    <thead>
                        <tr>
                            <td>Photo de couverture</td>
                            <td>Titre</td>
                            <td>Auteur</td>
                            <td>Quantité</td>
                            <td>Catégorie</td>
                            <td>Frais</td>
                        </tr>
                    </thead>
                    <tbody id="contenu">
                        <?php foreach ($ouvrages as $ouvrage): ?>
                            <tr>
                                <td><img src="assets\imgs\<?= $ouvrage['picture'] ?>" class="ImgAdherent"></td>
                                <td><?= $ouvrage['titre'] ?></td>
                                <td><?= $ouvrage['auteur'] ?></td>
                                <td><?= $ouvrage['quantite'] ?></td>
                                <td><?= $ouvrage['categorie'] ?></td>
                                <td class="Mdps"><?= $ouvrage['frais'] ?></td>

                                <td>
                                    <div class="ButtonsUpSupp">
                                        <a href="UpdateOuvrage.php?idBook=<?= $ouvrage['idBook'] ?>" class="btn-update"><i class="fa-solid fa-pen-to-square"></i></a>
                                        <a href="DeleteOuvrage.php?idBook=<?= $ouvrage['idBook'] ?>" class="btn-delete"><i class="fa-solid fa-trash"></i></a>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach ?>
                    </tbody>
                </table>
            </div>

        </div>


<!-- Logout Modal -->
<div id="logoutModal" class="modal">
    <div class="modal-content">
        <h3>Confirmation de Déconnexion</h3>
        <p>Êtes-vous sûr de vouloir vous déconnecter ?</p>
        <div class="modal-buttons">
            <button id="confirmLogout" class="btn-yes">Oui</button>
            <button id="cancelLogout" class="btn-no">Non</button>
        </div>
    </div>
</div>

<!-- =========== Scripts =========  -->
<script src="assets/js/main.js"></script>

<!-- ====== ionicons ======= -->
<script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
<script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>


<script>
    //Recherche
    document.getElementById('searchInput').addEventListener('input', function() {
        let searchTerm = this.value.toLowerCase();  // Obtenir la valeur du champ de recherche
        let rows = document.querySelectorAll('#contenu tr');  // Obtenir toutes les lignes du tableau

        rows.forEach(function(row) {
            let cells = row.getElementsByTagName('td');
            let found = false;

            // Vérifier chaque cellule de la ligne
            for (let i = 0; i < cells.length; i++) {
                if (cells[i].innerText.toLowerCase().includes(searchTerm)) {
                    found = true;
                    break;  // Si on trouve une correspondance, on sort de la boucle
                }
            }

            // Afficher ou cacher la ligne en fonction du résultat
            if (found) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    });
</script>