<!-- ======= Styles ====== -->
<link rel="stylesheet" href="assets/css/style.css">
<link rel="stylesheet" href="assets/css/styleButtonAjouter.css">

<style>
    /* Button update et supprimer */
    .ButtonsUpSupp a {
        text-decoration: none;
        padding: 10px 15px;
        border-radius: 5px;
        display: inline-flex;
        align-items: center;
        font-weight: bold;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    /* Bouton Update (jaune) */
    .btn-update {
        background-color: #f1c40f;
        color: #fff;
    }

    .btn-update:hover {
        background-color: #f39c12;
        transform: scale(1.05);
    }

    /* Bouton Delete (rouge) */
    .btn-delete {
        background-color: #e74c3c;
        color: #fff;
    }

    .btn-delete:hover {
        background-color: #c0392b;
        transform: scale(1.05);
    }

    /* Style pour les icônes */
    a i {
        margin-right: 5px;
    }
</style>
        <!-- ================ Order Details List ================= -->
        <div class="details">
            <div class="recentOrders" style="min-height: 200px;">
                <div class="cardHeader">
                    <h2>Liste des adhérents</h2>
                    <a href="AjoutAdherent.php">
                        <button class="Btn">
                            <div class="sign">+</div>

                            <div class="text">Ajouter</div>
                        </button>
                    </a>
                </div>
                <!-- ================ LES TABLEAUX ICI ================= -->
                <table>
                    <thead>
                        <tr>
                            <td style="text-align: left;">Profile</td>
                            <td style="text-align: left;">Nom</td>
                            <td style="text-align: left;">Prenom</td>
                            <td style="text-align: left;">Email</td>
                            <td style="text-align: left;">Téléphone</td>
                        </tr>
                    </thead>
                    <tbody id="contenu">
                        <?php foreach ($adherents as $adherent): ?>
                            <tr>
                                <td><img src="assets\imgs\<?= $adherent['picture'] ?>" class="ImgAdherent"></td>
                                <td style="text-align: left;"><?= $adherent['nom'] ?></td>
                                <td style="text-align: left;"><?= $adherent['prenom'] ?></td>
                                <td style="text-align: left;"><?= $adherent['email'] ?></td>
                                <td style="text-align: left;"><?= $adherent['tel'] ?></td>
                                <td>
                                    <div class="ButtonsUpSupp">
                                        <a href="UpdateAdherent.php?idAdherent=<?= $adherent['idAdherent'] ?>" class="btn-update"><i class="fa-solid fa-pen-to-square"></i></a>
                                        <a href="DeleteAdherent.php?idAdherent=<?= $adherent['idAdherent'] ?>" class="btn-delete"><i class="fa-solid fa-trash"></i></a>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach ?>
                    </tbody>
                </table>

            </div>

        </div>


<script>
    //Recherche
    const searchInput = document.getElementById('searchInput');
    searchInput.addEventListener('input', function() {
        let searchTerm = this.value.toLowerCase();  // Obtenir la valeur du champ de recherche
        let rows = document.querySelectorAll('#contenu tr');  // Obtenir toutes les lignes du tableau

        rows.forEach(function(row) {
            let cells = row.getElementsByTagName('td');
            let found = false;

            // Vérifier chaque cellule de la ligne
            for (let i = 0; i < cells.length; i++) {
                if (cells[i].innerText.toLowerCase().includes(searchTerm)) {
                    found = true;
                    break;  // Si on trouve une correspondance, on sort de la boucle
                }
            }

            // Afficher ou cacher la ligne en fonction du résultat
            if (found) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    });
</script>
